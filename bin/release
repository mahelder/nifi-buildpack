BUILD_DIR=$1
NIFI_DIR="apache/nifi"

echo "---"

if [ ! -f $BUILD_DIR/Procfile ]; then
  cd $BUILD_DIR
  echo "default_process_types:"
  echo "  web: sh $BUILD_DIR/$NIFI_DIR/nifi-1.10.0/bin/nifi.sh && sh $BUILD_DIR/post-deploy.sh"
fi