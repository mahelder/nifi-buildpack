BUILD_DIR=$1

echo "---"

if [ ! -f $BUILD_DIR/Procfile ]; then
  cd $BUILD_DIR
  echo "default_process_types:"
  echo "  web: sh apache/nifi/nifi-1.10.0/bin/nifi.sh && sh $BUILD_DIR/post-deploy.sh"
fi