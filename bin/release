BUILD_DIR=$1
NIFI_DIR="apache/nifi"

echo "---"

if [ ! -f $BUILD_DIR/Procfile ]; then
  cd $BUILD_DIR
  echo "default_process_types:"
  echo "  web: sh bin/$NIFI_DIR/nifi-1.10.0/bin/nifi.sh && sh bin/post-deploy.sh"
fi