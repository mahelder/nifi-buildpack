BUILD_DIR=$1
NIFI_DIR="apache/nifi"

echo "---"

if [ ! -f $BUILD_DIR/Procfile ]; then
  cd $BUILD_DIR
  echo "default_process_types:"
  echo "  web: sh $NIFI_HOME/bin/nifi.sh && python $NIFI_HOME/post-deploy.py"
fi